# AI Assistant Notes

Before making changes:
- Read `docs/SAFETY.md` and `docs/QA_CHECKLIST.md`.
- Review active toggles in `src/utils/featureFlags.ts`.

After changes:
- Run `npm run sanity` and report results.
